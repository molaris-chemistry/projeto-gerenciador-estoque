// App.tsx — Expo Router gerencia a navegação via app/_layout.tsx
// Este arquivo é mantido para compatibilidade com o entry point legado (index.js).
export { default } from 'expo-router/build/qualified-entry';
